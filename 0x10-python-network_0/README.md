# 0x10-python-network_0

## 0-body_size.sh
This Bash script takes a URL as an argument, sends a request to the URL, and outputs the size of the response body in bytes.

### Usage
```bash
./0-body_size.sh <URL>
